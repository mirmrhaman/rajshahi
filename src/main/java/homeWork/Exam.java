/*
package homeWork;


public class Exam {

    public Boolean sumsToTarget(Integer[] arr, Integer target) {

        int arr ( int[] a)
            int sum = 0;
            for (int i = 0; i < a.length; i++) {
                sum += a[i];
            }


    }
}

 */


/*    Write a method that checks if there is at least one pair of numbers that sum equals to target.
        arr=[1, 3, 4] and target=5 result is true because the pair (1,4) sums to five.

        The method will have two parameters. First, the integer array where you will have a list of values, and the second parameter is the target that you are aiming to match with.

        Sample output:

        sumsToTarget([1, 3, 4], 5) -> true
        sumsToTarget([2, 4, 6], 5) -> false
        sumsToTarget([-1, 4, 2, 1], 0) -> true

        Method Structure:

public Boolean sumsToTarget(Integer[] arr, Integer target) {

        }

 */
